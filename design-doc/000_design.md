# 設計

Dzarn は OCaml 5.4 用のリンターである。Dzarn は以下のようなリントを行う：

- どこからも使用されていない公開関数を見つける。

以下のように起動すると、指定したディレクトリ配下にある全ての OCaml プログラム（\*.ml, \*.mli）を走査しリントする。：

```
dzarn <ディレクトリ>
```

デフォルトの挙動ではコード中の問題を見つけるだけで、修正は行わない。`--fix` オプションをつけて起動された場合、見つけた問題を修正する：

```
dzarn --fix <ディレクトリ>
```
